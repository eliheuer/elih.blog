---
// Redirect /blog/ to /
export function getStaticPaths() {
  return [
    { params: { page: undefined } }
  ];
}

// Always redirect to the root URL
return Astro.redirect('/', 301);
---
<!-- This file only handles redirects, no content needed -->
